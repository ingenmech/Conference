package com.epam.evm.conference.validator;

import org.junit.Assert;
import org.junit.Test;

import java.time.LocalTime;
import java.time.ZoneId;
import java.time.format.DateTimeFormatter;

public class DataTimeTest {

//    @Test
//    public void test(){
//        String  t = "10:30:00";
//        ZoneId id = ZoneId.systemDefault();
//        LocalTime now = LocalTime.now(id);
//        DateTimeFormatter nowFormatter = DateTimeFormatter.ofPattern("HH:mm");
//        String nowRow= now.format(nowFormatter);
//
//        DateTimeFormatter formatter = DateTimeFormatter.ISO_LOCAL_TIME;
//        LocalTime exTime = LocalTime.of(10, 30);
//        LocalTime acTime = LocalTime.parse(t, formatter);
//        String time = acTime.format(formatter);
//        Assert.assertEquals(exTime,acTime);
//        Assert.assertEquals(t,time);
//        Assert.assertEquals("20:11",nowRow );
//    }

}
